# 更新记录

## 0.1.7

- 添加多账号管理：分别扫码一次，保存登录后从下拉框切换；支持账号备注、重新扫码和忘记登录。
- 每个账号使用独立的 Windows DPAPI 登录文件和浏览器会话，好友 / 群聊、勾选、草稿、模板、历史及当天去重分别保存。
- 切换前核对服务端账号和私信账号，核对成功后才启用；失败或停止时保留原来的当前账号，切换不自动发送。
- 发送、同步或登录处理中禁止切换账号；停止后先完成结果记录，再恢复账号操作。
- 同一账号重新扫码或 Cookie 更新后沿用原档案；旧版当前账号升级时保留配置和历史。
- 登录失效只要求对应账号重新扫码，临时网络错误保留登录状态；忘记登录保留记录且不影响其他账号。
- 沿用 0.1.6 群聊修复。账号切换仍需要联网加载和核对，登录有效期由抖音决定。

## 0.1.6（合入 0.1.7，未单独发布）

- 修复群聊在同步阶段被过滤、无法勾选及发送的问题。
- 单聊和群聊按稳定会话 ID 和类型区分；群聊显示专门标识，新同步的群聊默认不勾选。
- 群聊文字 / emoji 使用已登录 IM SDK，图片及原生表情沿用聊天页发送。
- 发送前核对账号、群聊身份和会话类型；服务端回执必须与本次消息匹配，结果不明时不会自动重发。
- 群聊沿用每轮发送 / 跳过明细及当天去重；兼容 0.1.5 的好友勾选、专属内容和历史。
- 使用虚构数据做离线验证，真实群聊发送效果及火花状态仍需在抖音中核对。

## 0.1.5

- 修复仅统计跳过数量、没有保存跳过好友明细的问题。
- 每轮保存好友姓名、发送结果和跳过原因，关闭软件后仍可查询。
- 发送记录支持按轮次和结果筛选，可单独查看“已跳过”。
- 跳过明细与实际发送状态分开保存，保留原有成功记录和当天去重保护。
- 兼容旧版发送记录；旧版未保存的跳过明细不会自动补造。

## 0.1.4

- 文字 / emoji 改为通过已登录的 IM SDK 提交消息，无需逐个打开好友聊天页。
- 发送前核对单聊对象和账号，发送后核对本次消息的服务端回执。
- 登录 Cookie 更新后核对实际账号，保持原有好友、模板及去重记录。
- 保留图片和抖音原生表情的聊天页发送方式。
- 整理公开源码、功能与隐私说明，提供 Windows 安装包和便携包。

## 0.1.3

- 火花好友优先，数字天数从高到低排序。
- 支持点击头像、昵称、状态或整行空白勾选好友。
- 改善自动打开私信入口和加载遮罩后的重试。

## 0.1.2

- 增加可停止的慢网等待和实时进度。
- 登录、同步和发送复用同一个浏览器窗口；失败或超时后保留窗口。

## 0.1.1

- 更新“克克咪 火花搭子”名称、应用图标和界面标识。

## 0.1.0

- 初始 Windows 桌面版本：扫码登录、自选好友、自定义内容、模板与发送记录。
